# Define obfuscation keep rules that should be consumed by modules depending on ngf_framework.
# This file is intentionally empty for now; it exists because ngf_framework/build-profile.json5
# declares it in arkOptions.obfuscation.consumerFiles for release builds.
